algorithme ex
var mot,nbp,ph,nbc,nbv,nbm
debut
ecrire("donner une phrase")
lire(ph)
nbp:+length(ph)
  nbp = 0
  nbc = 0
  nbv = 0
  nbm=0
for (i=1,i>=length(ph),i++)
  if i=' '
  nbm+=1
for (i=1,i>=length(ph),i++)
if i in('a','e','i','o','y','u','A','E','I',"O','U,'Y')
nbv+=1
ecrire(la taille de la phrase est",nbp)
ecrire("le nombre de caractere est",nbm)
ecrire("le nombre de voyelle",nbv)